# Project and thread separate context boundaries

The learner explained project and thread through the lens of context: a project sets the file-level context Codex can see, while a thread holds the task-level conversation for a specific need. They also identified the important boundary that multiple threads do not replace task decomposition, because overlapping file edits or unclear task boundaries can still create interference.

**Evidence**: The learner connected project, thread, context, task decomposition, thread interference, and archiving into one explanation suitable for teaching a beginner.
